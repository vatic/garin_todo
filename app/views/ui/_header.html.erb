<nav class="navbar navbar-inverse navbar-fixed-top todos-navbar">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="#">TODO List</a>
    </div>
    <div id="navbar" class="collapse navbar-collapse">
      <ul class="nav navbar-nav todos-main-nav">
        <% if current_user.admin? %>
          <li>
            <%= link_to "Admin Panel", admin_dashboard_path%>
          </li>
        <% end %>

        <li>
          <form class="navbar-form navbar-left" role="search" action=<%= user_todos_path(current_user) %>>
            <div class="form-group">
              <input type="text" class="form-control" placeholder="Search Todos" name="search" value=<%= params[:search] %>>
            </div>
            <button type="submit" class="btn btn-default">Submit</button>
          </form>
        </li>

      </ul>

      <ul class="nav navbar-nav navbar-right todos-auth-nav">
        <% if user_signed_in? %>
          <li>
            <p class="navbar-text">Signed in as 
              <%= current_user.email %>
            </p>
          </li>
          <li>
            <%= link_to("Sign out", destroy_user_session_path, :method => :delete) %>
          </li>
        <% else %>
          <li>
            <%= link_to("Sign in", new_user_session_path, type: "button", class:"btn btn-default navbar-btn") %>  
          </li>
        <% end %>
      </ul>
    </div><!--/.nav-collapse -->
  </div>
</nav>
